# Frontend env vars (Vite exposes only VITE_* vars to the browser)
# Copy to .env and fill in values

# API base URL — in dev, Vite proxy handles this automatically
# In production, point to your deployed backend
VITE_API_BASE_URL=http://localhost:5000/api
